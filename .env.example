# 复制本文件为 .env 并填入真实值（.env 已被 gitignore，勿提交）
# Nacos 鉴权 token：openssl rand -base64 32 生成
NACOS_AUTH_TOKEN=换成一个随机的base64密钥
# Nacos 登录密码（对应默认账号 nacos）
NACOS_PASSWORD=你的nacos密码
# PostgreSQL 密码
POSTGRES_PASSWORD=postgres
# MySQL root 密码
MYSQL_ROOT_PASSWORD=root
